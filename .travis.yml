
# whitelist
branches:  
  only:
    - master
    - staging
    - production
services:  
#Enable docker service inside travis
  - docker
before_install:  
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
script: 
  #build the image
  - docker build --no-cache -t $DOCKER_IMAGE_NAME .
  #tag image
  - docker tag 3blades/openresty 3blades/openresty:build-$TRAVIS_BUILD_NUMBER
  #push image
  - docker push 3blades/openresty:build-$TRAVIS_BUILD_NUMBER
notifications:
  slack: 3blades:0lcWo2AurVy41XkVdyFVejCG
env:
  - $DOCKER_IMAGE_NAME="3blades/openresty"
